
<!-- Hero Section -->
<header class="hero-section mb-5">
    <div>
        <h1 class="hero-title">{{hero.title}}</h1>
        <p class="hero-description">{{hero.description}}</p>
        <a class="btn btn-primary" href="{{hero.buttonLink}}">{{hero.buttonText}}</a>
    </div>
</header>

<!-- Información Principal -->
<section class="container py-5 section-content">
    <div class="text-center mb-4">
        <img src={{logo.src}} alt={{logo.alt}} width="250rem" class="img-fluid">
        <h2 class="mt-3 font-weight-bold">{{mainSection.heading}}</h2>
    </div>
    <div class="text-center mb-5 mt-5">
        <h4 class="font-weight-bold">{{mainSection.subheading}}</h4>
        <p class="mt-2">{{mainSection.description}}</p>
    </div>
</section>

{{> servicesSection}}
{{> testimonialsSection}}
{{> teamSection}}
{{> contactSection}}